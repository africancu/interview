<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.xzt.interview.mapper.InterviewMapper">

    <sql id="Base_Column_List" >
    interview_id, category_id, name, begin_time, end_time, compere, description, type,
    status, pre_pic_url, pre_video_url, video_url, create_time, update_time
    </sql>

    <select id="findByinterviewId" resultType="cn.xzt.interview.DTO.SpeakerDTO">
        select
        speaker_id, interview_id, name
        from speaker
        where interview_id = #{interviewId,jdbcType=INTEGER}
    </select>

    <select id="selectByPrimaryKey" resultType="cn.xzt.interview.DTO.InterviewDTO" >
        select
        interview_id,  name, begin_time as beginTime, end_time as endTime, compere, description, type,
        status, pre_pic_url as prePicUrl, pre_video_url as preVideoUrl, video_url as videoUrl, create_time as createTime, update_time as updateTime
        from interview
        where interview_id = #{interviewId,jdbcType=INTEGER}
    </select>

    <select id="findAll" resultType="cn.xzt.interview.DTO.InterviewDTO" >
        select
        interview_id,begin_time as beginTime,end_time as endTime,  name,  compere, description, type,
        status, pre_pic_url as prePicUrl, pre_video_url as preVideoUrl, video_url as videoUrl
        from interview
        <choose>
            <when test=' status != null and "" != status '>
                where status = #{status}
            </when>
        </choose>
        order by create_time desc
    </select>

    <insert id="insertInterview" useGeneratedKeys="true" keyProperty="interviewId">
    insert into interview (interview_id, category_id, name,
      begin_time, end_time, compere,
      description, type, status,
      pre_pic_url, pre_video_url, video_url
      )
    values (#{interviewId,jdbcType=INTEGER}, #{categoryId,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR},
      #{beginTime,jdbcType=TIMESTAMP}, #{endTime,jdbcType=TIMESTAMP}, #{compere,jdbcType=VARCHAR},
      #{description,jdbcType=VARCHAR}, #{type,jdbcType=INTEGER}, #{status,jdbcType=INTEGER},
      #{prePicUrl,jdbcType=VARCHAR}, #{preVideoUrl,jdbcType=VARCHAR}, #{videoUrl,jdbcType=VARCHAR})

    </insert>

    <insert id="insertSpeaker">
        insert into speaker(interview_id,name) values (#{interviewId},#{name})
    </insert>

    <delete id="deleteSpeaker" >
        DELETE from speaker where interview_id =#{interviewId}

    </delete>

    <update id="updateInterview">
        update interview
        <set >
            <if test="categoryId != null" >
                category_id = #{categoryId,jdbcType=INTEGER},
            </if>
            <if test="name != null" >
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="beginTime != null" >
                begin_time = #{beginTime,jdbcType=TIMESTAMP},
            </if>
            <if test="endTime != null" >
                end_time = #{endTime,jdbcType=TIMESTAMP},
            </if>
            <if test="compere != null" >
                compere = #{compere,jdbcType=VARCHAR},
            </if>
            <if test="description != null" >
                description = #{description,jdbcType=VARCHAR},
            </if>
            <if test="type != null" >
                type = #{type,jdbcType=INTEGER},
            </if>
            <if test="status != null" >
                status = #{status,jdbcType=INTEGER},
            </if>
            <if test="prePicUrl != null" >
                pre_pic_url = #{prePicUrl,jdbcType=VARCHAR},
            </if>
            <if test="preVideoUrl != null" >
                pre_video_url = #{preVideoUrl,jdbcType=VARCHAR},
            </if>
            <if test="videoUrl != null" >
                video_url = #{videoUrl,jdbcType=VARCHAR},
            </if>

            update_time =now(),

        </set>
        where interview_id = #{interviewId,jdbcType=INTEGER}

    </update>
</mapper>