spring:
  datasource:
    url: jdbc:mysql://139.159.180.205:3306/interview?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  servlet:
    multipart:
     enabled: true
     max-file-size: 10MB
     max-request-size: 10MB



mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath*:mapper/*Mapper.xml

logging:
  config: classpath:logback-spring.xml

pagehelper:
  auto-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

nginx_url: http://192.168.0.71/

physics_url: D://upload/

nginx_port: 8090

os_win: D://temp.txt
os_linux: /opt/temp.txt

